- name: variables
  hosts: local
  connection: local
  vars:
    COURSE: "DevOps with AwS"
    DURATION: "120 hours"
    TRAINEE: "Krishna"
  tasks:
  - name: print the variables
    ansible.builtin.debug:
      msg: "Course is {{ COURSE }} with duration of {{ DURATION }} by trainee {{ TRAINEE }}"